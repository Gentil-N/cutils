#include <stdio.h>
/*
#include "../ram/ram.h"

void error_callback(int code, const char *file, int line)
{
    printf("ERROR: %i, %s, %i\n", code, file, line);
}

void trace(void *ptr, const char *file, int line)
{
    printf("TRACE: (file)%s (ptr)%p (line)%i\n", file, ptr, line);
}

int main()
{
    ram_set_error_callback(error_callback);
    printf("haha\n");
    ram_trace(trace);
    int *a = ram_malloc(sizeof(int));
    int *b = ram_malloc(sizeof(int));
    int *c = ram_malloc(sizeof(int));
    printf("haha\n");
    ram_trace(trace);
    ram_free(a);
    printf("haha\n");
    ram_trace(trace);
    ram_free(c);
    printf("haha\n");
    ram_trace(trace);
    ram_free(b);
    ram_set_error_callback(NULL);
    return 0;
}
*/
